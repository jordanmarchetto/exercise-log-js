name: CI/CD Pipeline

on:
  push:
    branches:
      - 'main'

jobs:
  # rspec_tests:
  #   runs-on: ubuntu-latest

  #   services:
  #     db:
  #       image: postgres:13
  #       ports:
  #         - 5432:5432
  #       env:
  #         POSTGRES_USER: postgres_user
  #         POSTGRES_PASSWORD: postgres_password
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5

  #   steps:
  #   - uses: actions/checkout@v2

  #   - name: Set up Ruby
  #     uses: ruby/setup-ruby@v1
  #     with:
  #       ruby-version: 3.4.8 

  #   - name: Install dependencies
  #     run: |
  #       gem install bundler
  #       bundle install --jobs 4 --retry 3

  #   - name: Set up the database
  #     env:
  #       DB_USER: postgres_user
  #       DB_PASSWORD: postgres_password
  #       DB_HOST: localhost
  #       DB_PORT: 5432
  #     run: bin/rails db:create db:schema:load RAILS_ENV=test  # Set up the test database

  #   - name: Run RSpec
  #     run: bundle exec rspec

  build:
    # needs: rspec_tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build -t exercise-log-api:latest .

      - name: Create external docker network
        run: |
          docker network create docker_test_network || echo "Network already exists"

      - name: Run Docker container in detached mode
        run: |
          docker compose up -d
        env:
          DB_USER: postgres_user
          DB_PASSWORD: postgres_password
          DB_PORT: 5432
          DB_DATABASE=exercise_log_development
          DB_HOST=localhost
          APP_PORT: 3000
          # EXTERNAL_DB_PORT: 5432
          # SECRET_KEY_BASE: 1 
          DOCKER_NETWORK_BACKEND: docker_test_network # frontend network not used

      - name: Check node server status
        run: |
          sleep 20  # Wait for server to start
          curl -f http://localhost:3000 || (echo "Node server did not start" && exit 1)

      - name: Stop and remove Docker container
        run: |
          docker compose down

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: |
            jordanmarchetto/exercise-log-api:${{ github.run_number }}
            jordanmarchetto/exercise-log-api:latest
          context: .

      - name: Create and Push Tag
        run: |
          TAG_NAME="v${{ github.run_number }}"
          echo "Creating tag: $TAG_NAME"

          # Check if the tag already exists locally to prevent errors on re-runs
          if ! git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
            git tag $TAG_NAME
            git push origin $TAG_NAME
          else
            echo "Tag $TAG_NAME already exists, skipping creation and push."
          fi
